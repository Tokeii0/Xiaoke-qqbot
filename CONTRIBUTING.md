# 参与开发

欢迎提交问题、文档改进和代码。请先阅读 [README](README.md) 完成本地安装，并使用独立的测试 QQ、群和数据目录。

## 提交改动

1. 说明要解决的问题，以及修改前后的具体行为。
2. 保持改动聚焦。新增 JEV 判断时，说明输入、置信度阈值、失败后的行为和额外调用成本。
3. 涉及权限、记忆范围、提醒、消息发送或持久化时，补充有意义的回归测试；普通文案改动不需要重复实现的测试。
4. 运行 `python -m unittest discover -s tests`。修改前端脚本时另运行 `node --check xiaoke_bot/web/app.js`。
5. 按 [开源提交检查](docs/OPEN_SOURCE.md) 审查暂存区，再提交 PR。

测试使用虚构账号、临时数据库和模拟接口。不要在测试中读取实际 `.env`、连接生产 QQ、发送真实群消息或调用付费模型。报告外部接口测试结果时说明模型、协议和日期，移除密钥及聊天内容。

前端是随 Python 包分发的原生 HTML、CSS 和 JavaScript，无需前端构建。新增资源类型时同步更新 `pyproject.toml` 和 `MANIFEST.in`。

## 问题报告

请提供操作系统、Python / Node.js 版本、复现步骤、预期与实际行为、经过脱敏的错误片段。可以用虚构对话重现 JEV 的误判；不要直接上传聊天数据库、后台请求详情或完整运行配置。

安全问题按 [SECURITY.md](SECURITY.md) 处理。

项目采用 [MIT](LICENSE) 许可证。提交贡献前，请确认你有权以该许可证提供相应代码和素材，并保留引用内容的上游版权信息。
