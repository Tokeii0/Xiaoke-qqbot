# 安全与隐私

机器人会处理群消息、成员标识、记忆、图片和模型请求。配置了第三方模型、JEV、搜索或远程记忆服务后，相关输入会发送给对应服务。部署者应向群成员说明启用的记录与外部服务范围。

## 部署边界

- 默认监听 `127.0.0.1`，后台登录只允许本机来源。远程管理优先通过 SSH 隧道访问，避免直接暴露管理接口。
- 分别生成 OneBot、Web 后台、记忆网关和可选 Webhook 的令牌。空的后台令牌会使管理后台不注册；记忆网关的令牌必须自行设置。
- `.env*`、本地网关配置和整个 `data/` 都是私有文件。`bot_config.json` 也可能含个人提示词、群号和 Webhook 令牌；密钥文件分离不代表其他文件可以公开。
- 请求日志即使隐藏了常见密钥，仍可能含完整对话、记忆、图片地址和私人信息。提交 Issue 时仅提供最小、脱敏的复现材料。
- 定期备份运行数据，并给备份设置与原文件相同的访问限制。

## 报告漏洞

请勿在公开 Issue、PR 或评论中提交真实令牌、可利用的线上地址或他人的聊天记录。仓库启用了 GitHub Private Vulnerability Reporting 时，通过 **Security → Report a vulnerability** 私下报告；未启用时，先请求维护者提供私密联系渠道，再发送敏感细节。

请说明影响的版本、复现步骤、所需权限和影响范围，尽量使用本地测试环境。此仓库尚未承诺固定的安全响应时限。

如果凭证已经进入远程 Git 历史，应先撤销或轮换凭证，再处理历史和受影响副本；新增 `.gitignore` 不能撤回已泄漏内容。
